<% provide(:button_text, 'Save changes') %>

<h1>Edit task</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
<%= render partial: 'form', locals: {url_path: user_task_path([@task.user, @task]),
  task: @task, method: 'patch'}%>
<%= link_to 'Back', user_tasks_path(@user) %>
</div>
</div>
